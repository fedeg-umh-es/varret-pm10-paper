# P2 Branch Provenance

## 1. Repository

P2 uses the Git repository `fedeg-umh-es/varret-pm10-paper`.
No new repository is created; no manual data migration between repositories has occurred.

## 2. Base Commit and Derivation

This P2 branch (`claude/p2-predictability-bound-ldvcbi`) was derived from the repository
history that includes the P4 documentary closeout snapshot (`390685f1f1312954ee67513f3e0db11b2670e7f9`).
Derivation is done exclusively to reuse existing observational data, predictions, metrics,
scripts, configurations, and traceability records already present in the repository.

Note: The P4 snapshot `390685f1f1312954ee67513f3e0db11b2670e7f9` and the tag
`archive/p4-documentary-closeout-2026-08-01` exist on the author's local machine.
In the remote execution environment, this branch is the designated P2 development branch
as established by the project workflow.

## 3. P4 Snapshot Immutability

The P4 historical snapshot (`390685f`) remains immutable. P2 work does not modify,
move, delete, or alter the snapshot, its associated tag, or the protected P4 branches.

Verified protected objects:
- Commits `fdb73b2`, `a181f7d`, `7731570`, `ab5eb4d`, `f1e993a` — present and unmodified
- Branch `origin/claude/p4-lightgbm-ems-audit-rjs3ch` — untouched

## 4. Purpose of Derivation

The derivation is exclusively for:
- Reading observational CSV files (pm10_daily.csv, pm10_valencia_vivers.csv, pm10_zarra_emep.csv)
- Reading existing model predictions (predictions.csv, predictions_valencia_vivers.csv, predictions_zarra_emep.csv)
- Reading skill metrics from evidence/paper_a/aggregates/master_diagnostic_table.csv
- Reproducing skill definitions and persistence baselines from existing scripts

## 5. P2 as a Separate Scientific Line

P2 is a separate scientific line with:
- Own branch: `claude/p2-predictability-bound-ldvcbi`
- Own directories: `docs/p2/`, `src/p2/`, `scripts/p2/`, `tests/p2/`, `results/p2/`
- Own outputs, tests, and manifests
- Own scientific question distinct from P4

## 6. P2 Output Isolation

All new outputs generated by P2 are stored exclusively under `results/p2/`, `docs/p2/`,
`src/p2/`, `scripts/p2/`, and `tests/p2/`. No existing files outside these directories
are modified.

## 7. Independence from Paper A

No modification in P2 shall be interpreted as a revision of Paper A (paper_a.tex,
paper_a_ems.tex, or any canonical Paper A artifact). P2 is an independent analytical
line addressing a different scientific question.

## 8. PR #18 Status

PR #18 (associated with `claude/p4-lightgbm-ems-audit-rjs3ch`) is closed without merge
and remains so. P2 does not reopen, reference, or incorporate that PR's artifacts.

## 9. Audit Commits Not in P2

Commits `fdb73b2`, `a181f7d`, `7731570`, `ab5eb4d`, `f1e993a` (the post-documentary
audit) are not cherry-picked, rebased, or merged into P2. They remain on their
respective branch.

## 10. No Manual Data Migration

No CSV files, prediction files, or scientific results have been copied manually between
repositories. All inputs are read from their canonical paths within `varret-pm10-paper`.
