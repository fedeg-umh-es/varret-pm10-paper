# Graphical abstract specification

`GRAPHICAL_ABSTRACT_STATUS = MANUAL_AUTHOR_CREATION_REQUIRED`

The graphical abstract must be created manually by the authors. Do not
use generative AI or AI-assisted image editing for the submitted artwork.

Use a simple three-stage vertical flow:

```text
Conventional error eligibility
             ↓
Dynamic-fidelity audit
             ↓
Eligibility changes
```

The author may add the canonical decision consequence `277 → 8` and
`269 changed cells (97.1%)`. The artwork should contain no additional
statistics, claims, or decorative scientific imagery.

The existing `figures/graphical_abstract_eligibility.pdf` is an
`AI_ASSISTED_DRAFT_NOT_FOR_SUBMISSION`. It is retained only as an
internal draft and is excluded from the final Overleaf archive.
